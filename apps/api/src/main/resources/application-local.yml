nexa:
  cors:
    allowed-origins: ${NEXA_CORS_ALLOWED_ORIGINS:http://localhost:8000}
  security:
    jwt:
      secret: ${NEXA_JWT_SECRET:nexa-local-development-secret-change-before-deployment}

spring:
  flyway:
    locations: classpath:db/migration,classpath:db/local-migration
